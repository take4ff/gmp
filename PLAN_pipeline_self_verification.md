# パイプライン自己検証プラン（信頼性担保のためのステージ別チェック）

作成日: 2026-07-19 / 対象バージョン: `transformer_260707`（正典）

## 背景・課題

これまでに、本体のパイプラインで**クラッシュせず結果はもっともらしい値を出し続ける**
静かなバグが繰り返し見つかっている。

0. **`raw_path`切り詰めバグ**（2026-07-10発見、[[raw_path_truncation_bug]]）: DB保存用
   `raw_path`を先頭500文字に切り詰めていたため、`USE_SUBSTITUTION_HEAD`が末尾の
   断片を誤って最終ステップとして扱っていた。実測で全サンプルの約46%
   （2022年以降はほぼ全件）が影響を受けていたが、学習・評価は最後まで正常に完走し、
   数値も一見妥当に見えていた。RAW_PATH_TRUNCATE_LEN撤廃で解消済みだが、発見までに
   複数の分析結果（base_after/aa_after指標、PETRAリーク率）が誤った前提の上で
   蓄積されていた。
1. **分岐グルーピング分断バグ**（2026-07-18、`7c87286`）: 同一祖先履歴を持つサンプルを
   any-of-setとして集約するグルーピングが、実際にはミニバッチ内でしか機能していなかった
   （学習時shuffle=Trueで約92%が誤って単一サンプル扱いに分断）。
2. **fold間checkpoint引き継ぎバグ**（2026-07-18、`8d3301f`）: `--folds`で不連続/単体fold
   実行時、事前学習トリガーの判定がループ内位置(`idx==0`)ベースになっており、実際の
   fold番号(`fold_id==1`)と食い違うケースで誤ったcheckpointを引き継いでいた。

3件に共通するのは、**クラッシュせず、結果はもっともらしい値を出し続けた**という点。
かつ、いずれも「実データ・実フロー（データの時期分布、shuffleの有無、`--folds`の
組み合わせ）でしか顕在化しない」性質を持つ。全実装を網羅的にテストするのは非現実的
なため、目指すゴールを再定義する：

> 「バグが一切ないことの証明」ではなく、**パイプラインを明示的なステージに分解し、
> 各ステージが想定通り動いた証拠を実行のたびに自動で残す**こと。

これにより「今回の学習は各ステージの自己診断を全てパスした」という形で信頼性を
示せることを目標とする。

## 基本方針（3レイヤー）

| レイヤー | 内容 | 長所 | 短所 |
|---|---|---|---|
| ① プリフライトチェック | 学習/walk_forward起動前にDB上で数分走らせ、split境界・グルーピング率・checkpoint整合性等を集計・assertする | 実データでの検証、今回の2バグを両方即座に検知できたはず | DB規模次第で数分〜数十分かかる |
| ② 埋め込み型ランタイムassert | `dataset.py`・`main.py`等の本体コード内に恒久的チェックを仕込む（`config.DEBUG_VERIFY`的フラグ） | 常時有効、プリフライトをすり抜けるケース（`--folds`の組み合わせ変化等）にも効く | 本体コードに変更が入る、保守対象が増える |
| ③ pure関数のユニットテスト | `assign_wf_splits`・グルーピングキー計算・`Feature_path_fast`等をDB不要のpytestで固定 | 一瞬で回る、コード変更時の回帰網 | 今回の2バグ（データ分布依存・実行フロー依存）は単体テストだけでは捕まえにくい |

**優先順位・着手レイヤーは未決定**（本ドキュメント作成時点でユーザーと未合意）。

既存資産: `transformer_260707/scripts/analysis/verify/`（`verify_batch_local_grouping.py`
等）は、今回のバグを事後的に発見した一回性の診断スクリプト群。これらは①プリフライト
チェックと同種の検証を既に個別に実装済みなので、「一回性の診断」から「毎回自動で
走る資産」へ格上げする方向で活用できる。

## パイプラインのステージ分解

| # | ステージ | 保証したい性質 | 今回の実例／関連バグ | 検証方法の候補 | 状態 |
|---|---|---|---|---|---|
| 1 | 前処理・DB構築 | `raw_path`の完全性、`labels`/`x_cat`が切り詰め前の完全な履歴文字列から一貫して計算されている | [[raw_path_truncation_bug]]（解消済み） | サンプリングして`raw_path`長分布・非切り詰め率を集計 | 未着手 |
| 2 | split割当 | 各foldのtrain/test日付境界に重複・漏れがない、リークがない | [[data_quality_collection_date_bug]] | `split_type_wf`の日付範囲をfold定義と突合 | 未着手 |
| 3 | グルーピング | 同一祖先履歴サンプルがsplit全体でグローバルにグループ化されている | 分岐グルーピング分断バグ（`7c87286`） | グループサイズ分布・分断率を集計、`verify_batch_local_grouping.py`を常時実行化 | 一回性スクリプトのみ存在 |
| 4 | dataloader/collate | バッチ内容がグループ定義と矛盾しない（Hard/Soft Target両モードで） | 上記と同根 | バッチ内`raw_y`の一貫性チェック | 未着手 |
| 5 | fold間引き継ぎ | 読み込むcheckpointが期待するfold番号のものである | fold間checkpoint引き継ぎバグ（`8d3301f`） | ロード時にfold番号をログ・assert（修正で一部対応済み、RuntimeError化済み） | 一部対応済み |
| 6 | 学習・評価 | 損失・指標が異常値でない、リークが起きていない | [[petra_comparison]]のリーク分析 | 既存の`verify_point_in_time_freq.py`等を定型化 | 一回性スクリプトのみ存在 |

## 実行ごとの自己診断ログ構想（案）

各run（`main.py`実行やwalk_forward各fold）の出力ディレクトリに `stage_verification.json`
のような自己診断結果を残すイメージ:

```json
{
  "stage_1_db_integrity": {"status": "pass", "raw_path_truncated_ratio": 0.0},
  "stage_3_grouping": {"status": "pass", "fragmentation_ratio": 0.03},
  "stage_5_checkpoint_chain": {"status": "pass", "loaded_fold": 2, "expected_prior_fold": 2}
}
```

これにより「このrunは全ステージパスした」と機械的に示せる。プリフライト失敗時の
挙動（warnで続行 or abort）も要検討事項。

## 未決定事項（要ユーザー合意）

- どのレイヤー（①②③）から着手するか
- ステージ1〜6のうちどこから具体的なチェック項目を実装するか
- プリフライトチェック失敗時の挙動（警告のみ／実行中断）
- 既存`verify/`スクリプト群を①にどう統合するか（そのまま定期実行化 / 書き直し）

## 次のアクション

未定。次回セッションでユーザーと優先順位を確認してから実装に着手する。
